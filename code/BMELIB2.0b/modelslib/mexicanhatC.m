function [C]=mexicanhatC(D,param);% mexicanhatC                 - mexicanhat covariance model (Jan 1,2001)%% Compute the mexicanhat covariance matrix from a distance matrix.% Mexicanhat covariance is in the form of% C=param(1)*(1-param(2)*D^2)*exp(-(param(3)*D)^2);% % SYNTAX :%% [C]=mexicanhatC(D,param);%% INPUT :%% D       n by m   distance matrix, having real values >=0.% param   1 by 3   vector of parameters for the model%% OUTPUT :%% C       n by m   covariance matrix with same size as D.C=param(1)*(1-param(2)*D.^2).*exp(-(param(3)*D).^2);